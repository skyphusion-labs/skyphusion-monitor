# Inserted into the CodeQL default-setup / Code Quality dynamic workflow before
# analysis. TypeScript extraction needs node on PATH; fleet runners do not ship it.

- name: Setup Node.js
  uses: actions/setup-node@v4
  with:
    node-version: '22'

- name: Install dependencies
  run: npm ci
